{
  "name": "add-zenhub-release-action",
  "version": "1.0.0",
  "description": "Add an issue to Zenhub release. If the realase doesn't exist, it would be created automatically.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3"
  }
}
